# NoitaCA 设计文档体系 · 交付清单（2026-07-07 收口）

> 主理人：程驭（技术总监）｜团队：13 人专家团全建制
> 状态：**ADR D1–D5 全部钉死、跨文档一致性全绿、协议 wire 已落地**

## 一、本次收口动作（续前次 ADR 收口）

| # | 任务 | 负责人 | 结果 |
|---|------|--------|------|
| #18 | `OuterMessage.proto` 落盘（InputPayload 8B 无 SpellId + `G2C_MatchSettle` 精简版 + `C2G_DeviceReport`） | 端木林 | ✅ 已落盘并自检 |
| #27 | `法术系统设计.md` 对齐 ADR §7（Ice→P1、Shield 挡伤不抗推） | 谋远 | ✅ 逐行核对天然一致，无需回退 |
| #28 | `UIUX交互设计.md` 去 SpellId×4 + 去 HP 血条×6 + Dash 改无 i-frames | 涂彩 | ✅ 逐行读盘验证通过 |
| #29 | `数值平衡设计.md` Dash i-frames 对齐 ADR §3 | 舒衡 | ✅ "无 i-frames（纯位移，不免疫任何效果）" |
| #34 | `数据分析埋点规格.md` iframes_active 对齐 + 升 v1.1 混合派生模型 | 娄数 | ✅ 主理人裁定采用方案 A |
| #35 | `游戏设计文档.md` §7 道具表对齐 no-HP（净化瓶 / 挡伤不免疫击退） | 程驭（代落盘） | ✅ 已 Edit |
| — | `结算界面规格.md` 升 v1.2（wire+派生闭环，对齐协议 §6.1） | 涂彩 | ✅ |
| — | `多人联机帧同步对战设计.md` §7.4.4 道具表与 ADR 对齐 | 程驭 | ✅ |
| — | `跨平台输入架构.md` line 524/666 精确化为 D3 口径 | 程驭 | ✅ |
| — | `协议与序列化规范.md` §6.1 `G2C_MatchSettle` 规范（端木林先落，涂彩后对齐） | 端木林/涂彩 | ✅ |

## 二、待拍板事项 · 主理人裁定

| 事项 | 裁定 |
|------|------|
| 赛后结算 wire 形态 | **精简 wire + 客户端派生**：仅 `MmrDelta` 服务端权威下发；`Rank/Kills/Deaths/IsWinner/IsDraw/MaterialContribution` 客户端本地 sim 派生（确定性、各端一致）。减包、收敛 desync 面，与确定性红线一致。 |
| 埋点事件派生架构（娄数提案 A/B） | **A）混合 verify 模型（MVP 采用）**：服务端权威子集（输入边沿+时间线+状态机，无需仿真）+ 客户端上报经既有状态哈希多数派裁决校验。B）服务端 100% 权威需搬 net8 headless shadow sim，列 post-MVP 加固。 |
| 像素字体资产 owner（涂彩 `结算界面规格` §9.3 Q5） | **指派技术美术 柯桥（TA）**，经 YooAsset 管线产出，视觉还原前置依赖闭环。 |
| `SpellId` 孤儿字段（A4） | 维持**删除**裁定（ProtocolVersion 1→2，payload 12B→8B）。全文档仅保留 `PlayerData.SpellIds[]`（法术槽 loadout，由 `SelectedSpellSlot` 推导），与已删 per-frame 字段无歧义。 |

## 三、跨文档一致性核验矩阵（D1–D5 + 确定性红线）

| 决策点 | 预期 | 核验结果 |
|--------|------|----------|
| D1 边界死亡 | 出界即死默认 | ✅ GDD §4 / 结算 / 竞技场 / 叙事全一致 |
| D2 缩圈 | MVP 不缩圈 | ✅ 仅 NUMERIC 含完整 kill-ring 模型且标注"v1 默认不做/P1 可选" |
| D3 Dash | 保留、纯位移、无 i-frames | ✅ ADR/GDD/NUMERIC/UI_UX/ART/AUDIO/TEST/WORLDVIEW 八份统一；输入架构泛例已收口 |
| D4 无 HP | 纯出界即死、无血条 | ✅ UI_UX 血条已移除、GDD §7 道具表对齐、铁律 §9.7 禁止免推 |
| D5 AimPower+循环切换 | 固定力度 + 循环切法术 | ✅ 输入架构 / 协议 / 音频 Switch 一致 |
| 确定性红线 | 不进 InputPayload / 不进哈希 | ✅ 暂停/快捷聊天/结算按钮均不进流；埋点事件显式"红线不变" |
| 随机 | 绑定 Xorshift128Plus PRNG | ✅ 音频 cosmetic 微随机显式划定不回馈模拟、不进哈希 |

> 唯一保留的"无敌"表述：`帧同步Netcode设计.md` 的"断线玩家无敌 3s"＝**重连网络容错**（防止掉线被击杀 farming），非战斗无敌帧，与 D3/D4 不冲突，保留。

## 四、协议 / 工程落点

- `Tools/NetworkProtocol/Outer/OuterMessage.proto`：已含 `C2G_Input`(MemoryPack 30Hz) / `InputPayload`(8B) / `G2C_FrameBatch` / `PlayerInput` / `G2C_SpectatorBatch` / `G2C_MatchSettle`(ProtoBuf) / `SettlePlayer` / `C2G_DeviceReport`。
- ProtocolVersion = **2**（因删除 SpellId bump）。
- 待实现阶段：`ProtocolExportTool` 重新生成双端代码（构建与部署 已写流程卡点）。

## 五、文档体系总览（Docs/ 23 份）

**决策层**：`架构决策记录.md` · `设计审查_2026-07-07.md` · `协议与序列化规范.md`
**设计/系统**：`游戏设计文档.md` · `法术系统设计.md` · `数值平衡设计.md` · `生物AI规格.md` · `UIUX交互设计.md` · `跨平台输入架构.md` · `结算界面规格.md`
**美术/音频/叙事**：`美术风格指南.md` · `美术管线与VFX规范.md` · `音频设计规格.md` · `世界观与叙事.md`
**关卡/QA/构建/数据**：`竞技场关卡设计.md` · `测试QA计划.md` · `构建与部署.md` · `数据分析埋点规格.md`
**技术基础**：`多人联机帧同步对战设计.md` · `帧同步Netcode设计.md` · `玩法重构方案.md` · `项目搭建与贡献指南.md` · `文档体系调研报告.md` · `开发智能体配置.md`

## 六、未阻塞的 P2 暂缓项（原档B，上线前补）

本地化、商业化、desync 监控 dashboard、观战模式（`G2C_SpectatorBatch` 已预留注释位）、HP 可选模式（Mode B 数据/UI 已留通道）。均不阻塞 MVP。

---
**结论**：设计文档体系已可在 ADR 单一事实源下进入实现阶段；协议 wire 已定稿待导出；跨文档零冲突。
