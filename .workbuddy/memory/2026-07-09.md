# 2026-07-09

## Docs 文档中文化改名
- 将 `Client/Assets/Docs/` 与根目录 `Docs/` 下全部 27 个英文 .md 文档改为中文文件名（如 `GAMEPLAY_REFACTORING_PLAN.md` → `玩法重构方案.md`）。
- 流程：先做全项目内容替换（旧文件名令牌 → 中文名，\b 单词边界 + 忽略大小写，覆盖 .md/.cs/.proto 等文本文件），再重命名文件，避免断链。
- 校验：全项目扫描旧令牌残留 = 0；Docs/ 内部及跨目录 markdown 链接零断链（`fantasy_quickstart.md` 的 8 处断链是 Fantasy 框架外部文档，与本次无关）。
- 注意：`Docs/` 中部分文档标题括号里原本带有英文文件名（如 `（ANALYTICS_METRICS_SPEC）`），替换后变为中文，属正常副作用。

## 创建 NoitaCA 研发专家团（expert-manager）
- 用 expert-manager skill 基于项目架构创建 Team 型专家团 `noitaca-dev`（8 人：1 主理人 + 7 团员），已校验+注册到 `~/.workbuddy/plugins/marketplaces/my-experts`，categoryId=`03-GameSpatial`。
- 角色（沿用项目已有花名）：主理人 齐活林(研发交付总监)、许清楚(游戏策划/game-designer)、高见远(客户端技术架构师/tech-architect)、甄一同(帧同步网络工程师/netcode-engineer)、沙元格(元胞模拟工程师/simulation-engineer)、华丹青(技术美术/technical-artist)、易点通(交互体验设计师/ux-designer)、严过关(测试工程师/qa-engineer)。
- 主理人内置 4 个 SOP Workflow：A 新增材料/反应、B 新增联机功能/法术/协议、C 架构/重构评审、D 单维度直调路由表。
- 坑：ImageGen 并行生成会因同秒时间戳文件名冲突而互相覆盖 → 必须逐张串行生成后立即改名。头像单张约 1.1-1.3MB（超 500KB 指南值但 validate 不拦，市场会缩放）。
- 坑：Windows 下 `rm` 绝对路径被 safe-delete 包装器拼错 cwd 前缀失败 → 改用 PowerShell `Remove-Item -LiteralPath`。

## Docs 全量设计审查（2026-07-09）
- 以齐活林(主理人)身份 TeamCreate `design-review-2026-07-09`，并行调度 7 位领域专家(许清楚/高见远/甄一同/沙元格/华丹青/易点通/严过关)逐行审查 Docs/ 全部 27 份文档。
- 产出整合报告 `Docs/设计审查_2026-07-09.md`。核心结论：历史 A1/A4/A5/B1/B5 真闭环；A2/A3 文档闭环但新手指引文案陈旧；B2 规则层闭环、规范层缺口；**B4 实质未闭环(升 P0)**。
- 新发现 7 个 P0：① 玩法重构方案 PlayerData.ActionFlags 仍 byte(协议已 ushort)→回滚截断 desync；② 玩法重构方案 ScheduleParallel 用持久化共享 RNG→跨线程竞争破坏确定性；③ 法术系统设计§6 vs 数值平衡§6.4 冷却/耗蓝/击退全表未对齐(数值自述"1:1"为假)；④ 确定性常量热更落点未定义→热更 desync；⑤ UIUX§5.1 残留 HP 行违 D4；⑥ 移动端缺 Jump/Pickup/Drop 映射违动作数守恒；⑦ 交付清单"全绿"与实际不符。
- 已就地修复：甄一同改了竞技场非法 hex + 帧同步Netcode 5 处(带宽12B→8B/56B、B3阻塞依赖标注、SpectatorBatch、GC注、断线无敌确定性约束)；华丹青 B2 Telegraph 规范草稿 v2 进行中(UX 已拍板提前量 Bomb36/Lava48/Poison30/Blink30)。
- 待用户拍板开放项：O1 Mode B(HP可选)是否移出 v1；O3 确定性常量热更策略(AOT vs bump PV)；O4 灰描边 RGB565 偏紫值(待高见远给)；O6 断线"HP冻结"改状态位。
